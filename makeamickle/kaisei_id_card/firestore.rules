rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 開発初期段階のテスト用ルール
    // Phase 0: エミュレータでの基本動作確認用
    // TODO: Phase 1でセキュリティルールを実装予定
    
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}